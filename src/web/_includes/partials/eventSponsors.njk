<div class="container shadow-1 rounded-lg border-transparent px-2 md:px-4 lg:px-4 xl:px-4 2xl:px-4 py-8 md:p-8 fg-inverse bg-inverse mx-auto">
   <div class="flex justify-around">
   {% for sponsor in event.sponsors %}
        <div class="px-2 md:px-4 lg:px-4 xl:px-4 2xl:px-4">
           <a href="{{ sponsor.url }}" target="_blank"> <img class="h-12" src="{{ sponsor.logo.src }}" alt="{{ sponsor.logo.alt }}" /></a>
        </div>
    {% endfor %}
    </div>
</div>