<div class="max-w-lg mx-auto shadow-1 rounded-lg border-transparent px-2 md:px-4 lg:px-4 xl:px-4 2xl:px-4 py-4 fg-inverse bg-inverse">
   <div class="pb-2 text-base sm:text-lg font-semibold">Milestones</div>
   <div class="grid grid-cols-2 gap-2 sm:gap-4">
   {% for milestone in event.timeline %}
        <div>
            {{ milestone.date }}
        </div> 
        <div class="font-semibold">
            {{ milestone.milestone }}
        </div>
    {% endfor %}
    </div>
</div>